import 'package:flutter/material.dart';

class DataHomeDio extends StatelessWidget {
  const DataHomeDio({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
